---
dependency:
  name: galaxy
driver:
  name: docker
platforms:
  - name: instance
    image: "docker.pkg.github.com/docker-hub-tm/ansible-test/${MOLECULE_DISTRO:-centos8}:latest"
    registry:
      url: docker.pkg.github.com
      credentials:
        username: ${USERNAME}
        password: ${PASSWORD}
    pre_build_image: True
provisioner:
  name: ansible
verifier:
  name: ansible
scenario:
  test_sequence:
    - dependency
    - syntax
    - create
    - converge
    - idempotence
    - verify
